# Where the API lives. No trailing slash.
API_URL=http://localhost:4000/api/v1

# Signs the session cookie. Generate with: openssl rand -base64 48
SESSION_SECRET=dev-only-session-secret-min-32-characters
